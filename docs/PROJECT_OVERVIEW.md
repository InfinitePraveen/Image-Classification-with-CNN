# Project Overview

The project follows a practical computer-vision pipeline: understand the dataset, explore class/image distributions, prepare inputs, augment training images, train a CNN, evaluate it, and expose inference through Flask.

The design intentionally favors transparent notebooks over a custom Python package so that a student can inspect every stage directly.
