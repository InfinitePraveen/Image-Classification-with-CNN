# Reproducibility

For reproducible experiments:

- use a fixed random seed;
- record Python, TensorFlow and NumPy versions;
- record epochs, batch size and learning rate;
- keep the train/validation split deterministic;
- preserve the notebook order;
- save the final model and evaluation metrics.
