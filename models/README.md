# Models

The trained model is generated by `06_inference_and_export.ipynb` and saved as:

```text
models/cifar10_cnn.keras
```

Large model binaries are ignored by Git. To reproduce the model, run the training and export notebooks.
